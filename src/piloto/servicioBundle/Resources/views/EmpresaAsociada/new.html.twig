<h1>EmpresaAsociada creation</h1>

<form action="{{ path('empresaasociada_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('empresaasociada') }}">
            Back to the list
        </a>
    </li>
</ul>
