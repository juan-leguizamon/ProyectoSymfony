<h1>EmpresaAsociada list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nit</th>
            <th>Nombre</th>
            <th>Direccion</th>
            <th>Pais</th>
            <th>Ciudad</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('empresaasociada_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nit }}</td>
            <td>{{ entity.nombre }}</td>
            <td>{{ entity.direccion }}</td>
            <td>{{ entity.pais }}</td>
            <td>{{ entity.ciudad }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('empresaasociada_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('empresaasociada_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('empresaasociada_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
