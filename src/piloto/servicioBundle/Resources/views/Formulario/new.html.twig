<h1>Formulario creation</h1>

<form action="{{ path('formulario_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('formulario') }}">
            Back to the list
        </a>
    </li>
</ul>
