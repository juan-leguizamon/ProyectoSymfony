<h1>TipoRemitente list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Idrmitente</th>
            <th>Idempresaasociada</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('tiporemitente_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.idRmitente }}</td>
            <td>{{ entity.idEmpresaAsociada }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('tiporemitente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tiporemitente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('tiporemitente_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
