<h1>FormasEnvio list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Aereo</th>
            <th>Terrestre</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('formasenvio_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.aereo }}</td>
            <td>{{ entity.terrestre }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('formasenvio_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('formasenvio_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('formasenvio_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
