<h1>Paquete list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Telefono</th>
            <th>Destino</th>
            <th>Idpoliticacosto</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('paquete_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.telefono }}</td>
            <td>{{ entity.destino }}</td>
            <td>{{ entity.idPoliticaCosto }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('paquete_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('paquete_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('paquete_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
